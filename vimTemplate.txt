

~ ;; toggle between upper- and lower-case
control + G  ;; shows line and column number... how to keep this?



;; 'set' options
;; colon (:) followed by set mycommand
:set tabstop = 4 ;; tab = four spaces instead of eight
:set expandtab ;; tabs are converted to spaces (useful since not every
computer handles tabs the same way, but note that makefiles will not
work without actual tabs.
:set  
